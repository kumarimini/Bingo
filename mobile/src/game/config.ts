// Point this at your running server (see /server). For a physical device on the
// same Wi-Fi, replace 'localhost' with your machine's LAN IP, e.g. 192.168.1.20.
export const SERVER_URL = process.env.EXPO_PUBLIC_SERVER_URL || 'http://localhost:4000';
